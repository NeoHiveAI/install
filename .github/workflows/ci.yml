name: CI

on:
  push:
  pull_request:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ShellCheck install.sh
        run: shellcheck install.sh
      - name: ShellCheck smoke.sh
        run: shellcheck test/smoke.sh
